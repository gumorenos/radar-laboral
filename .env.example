# Copy to .env on the deployment host. Never commit the real secret.
RADAR_ADMIN_TOKEN=replace-with-a-long-random-secret
RADAR_MAX_BACKFILL_DAYS=366
# Number of closed historical days Radar expects to have verified. Today is excluded.
RADAR_COVERAGE_DAYS=365
